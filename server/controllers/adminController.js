// controllers/adminController.js — ADMIN lead actions
// Responsibilities: list all leads (with search & pagination), update lead status

'use strict';

/**
 * @desc    Get all leads (supports ?search=&page=&limit=)
 * @route   GET /api/admin/leads
 * @access  Private (admin secret)
 */
// TODO: exports.getAllLeads = async (req, res, next) => { ... };

/**
 * @desc    Update lead status
 * @route   PATCH /api/admin/leads/:id/status
 * @access  Private (admin secret)
 */
// TODO: exports.updateLeadStatus = async (req, res, next) => { ... };
