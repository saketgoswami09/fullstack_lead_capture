// src/services/adminService.js — Admin API calls
// All requests include x-admin-secret header

// TODO: import axios from 'axios';

// const adminApi = axios.create({
//   baseURL: import.meta.env.VITE_API_BASE_URL,
//   headers: { 'x-admin-secret': import.meta.env.VITE_ADMIN_SECRET },
// });

/**
 * Fetch all leads — supports ?search=&page=&limit=
 */
// TODO: export const fetchLeads = (params) => adminApi.get('/admin/leads', { params });

/**
 * Update a lead's status
 * @param {string} id
 * @param {'New'|'Contacted'|'Closed'} status
 */
// TODO: export const updateLeadStatus = (id, status) => adminApi.patch(`/admin/leads/${id}/status`, { status });
